{
  "name": "englift",
  "source": [
    "index.html",
    "login.html"
  ],
  "scripts": {
    "start": "parcel . --port 1234 --open chrome",
    "build": "parcel build index.html login.html --public-url / --no-scope-hoist --no-content-hash && copy sw.js dist\\sw.js && copy .htaccess dist\\.htaccess && copy robots.txt dist\\robots.txt && copy sitemap.xml dist\\sitemap.xml"
  },
  "dependencies": {
    "@material-symbols/font-400": "^0.42.3",
    "@supabase/supabase-js": "^2.99.1"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.16.4",
    "@parcel/transformer-webmanifest": "^2.16.4",
    "buffer": "^6.0.3",
    "parcel": "^2.16.4"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
